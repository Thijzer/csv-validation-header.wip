columns:
  code:
    string:
  sort_order:
    integer:

rows:
  unflatten:
    separator: '-'
  nullify: ~

format:
  delimiter: ';'
  enclosure: '"'
  index: 'code'

validations:
  property:
    code:
      unique:
    sort_order:
      required:
      is_integer:
