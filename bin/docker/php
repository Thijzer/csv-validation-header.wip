#!/bin/sh

docker run -u 1000:1000 --init -it \
    -v ${HOST_COMPOSER_HOME:-~/.composer}:/.composer -e COMPOSER_HOME=/.composer \
    -v "$(pwd)":/app -w /app \
    -v /var/run/docker.sock:/var/run/docker.sock \
    --rm transtool:latest php -d memory_limit=4G "$@"